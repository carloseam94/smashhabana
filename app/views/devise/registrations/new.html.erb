    <%= stylesheet_link_tag 'login/style.css', media: 'all', 'data-turbolinks-track': 'reload' %>        
        
        <section class="signup">
            <div class="container">
                <div class="signup-content">
                    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                        <h2 class="form-title">Registrarse</h2>
                        <div class="form-group">
                            <%= f.text_field :full_name, autofocus: true, autocomplete: "full_name", class: "form-input", placeholder: "Nombre Completo" %>
                        </div>
                        <div class="form-group">
                            <%= f.text_field :username, autofocus: true, autocomplete: "username", class: "form-input", placeholder: "Nombre de Usuario" %>
                        </div>
                        <div class="form-group">
                            <%= f.email_field :email, autocomplete: "email", class: "form-input", placeholder: "Email" %>
                        </div>
                        <div class="form-group">
                            <%= f.password_field :password, autocomplete: "new-password" , class: "form-input", placeholder:"Contraseña"%>
                            <span toggle="#password" class="zmdi zmdi-eye field-icon toggle-password"></span>
                        </div>
                        <div class="form-group">
                            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-input", placeholder: "Repetir Contraseña" %>
                        </div>
                        <div class="form-group">
                            <%= f.submit "Registrarse", class: "form-submit"%>
                        </div>
                    <% end %>
                    <p class="loginhere">
                        Ya tienes una cuenta ?  <%= link_to  "Iniciar sesion", login_path, class: "loginhere-link" %>
                    </p>
                </div>
            </div>
        </section>

<script>
  document.addEventListener("DOMContentLoaded", () => { 
    $('body').css('background-image','linear-gradient(to left, rgba(245, 241, 200, 0.6), rgb(247, 233, 231)), url(<%= image_url('battlefield.jpg') %>)');
  });
</script>

